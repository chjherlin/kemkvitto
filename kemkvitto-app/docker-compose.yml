services:
  kemkvitto:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    dns:
      - 1.1.1.1
      - 8.8.8.8
    restart: unless-stopped
